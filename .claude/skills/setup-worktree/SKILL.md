---
name: setup-worktree
description: Bootstrap a fresh Claude worktree so previews and builds work — install web/admin dependencies, install the canonical .claude/launch.json with port-freeing preludes, use alternate ports so parallel worktrees don't collide. Use at the start of a session in a new worktree, when preview_start fails, when nuxt/node_modules is missing, or when ports 3002/5008 are already busy.
---

# Setup a fresh worktree

Fresh worktrees have no `node_modules` and a `launch.json` without port-freeing. Symptoms: `preview_start` fails, `EADDRINUSE`, `nuxt: command not found`.

1. **Dependencies** — only for the app(s) you'll actually run: `npm ci --prefix web` and/or `npm ci --prefix admin`.

2. **Launch config** — copy the canonical file: `cp .claude/skills/setup-worktree/assets/launch.json .claude/launch.json`.
   The port-freeing must be inline `bash -c` (the sandbox blocks executing script files under `.claude/` with EPERM). It kills only LISTEN-state owners of the port, so clients connected to something else are unaffected.

3. **Backend port** — verify API changes on **:5099**, not :5008 (5008 is frequently another worktree's API). The `api-5099` configuration runs `dotnet run --no-launch-profile` with `ASPNETCORE_ENVIRONMENT=Development` and the URL pinned to 127.0.0.1:5099. If the web app must call this API instead of the default, check `web/nuxt.config.ts` for the API base env var and set it on the web dev process.

4. **Branch hygiene** — the worktree branch is an autogenerated slug; rename it to `<type>/<issue>-<short-kebab>` before the first push (the `start-issue` skill covers this).
