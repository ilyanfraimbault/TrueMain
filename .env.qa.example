POSTGRES_USER=truemain_qa
POSTGRES_PASSWORD=truemain_qa
POSTGRES_DB=truemain_qa
RIOT_API_KEY=RGAPI-REPLACE_ME_QA
PGADMIN_DEFAULT_EMAIL=admin@truemain-qa.dev
PGADMIN_DEFAULT_PASSWORD=admin
NUXT_API_BASE_URL=http://api:8080

# Admin dashboard (admin/). Session secret must be >= 32 chars; credentials
# default to `truemain`/`truemain` and should be overridden for QA.
ADMIN_SESSION_PASSWORD=qa-admin-session-secret-min-32-characters
ADMIN_USERNAME=truemain
ADMIN_PASSWORD=truemain
