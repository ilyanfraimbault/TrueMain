namespace Core.Lol.Identifiers;

// Explicit, stable values (see PlatformRoute). 0 left unused so
// default(RegionalRoute) is an invalid sentinel.
public enum RegionalRoute
{
    Europe = 1,
    Americas = 2,
    Asia = 3,
    Sea = 4
}
